name: Tech Blog Auto Publisher

on:
  schedule:
    - cron: '0 21 * * *'   # 매일 KST 06:00 (UTC 21:00 전날)
  workflow_dispatch:         # 수동 실행 허용

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests feedparser googlenewsdecoder

      - name: Step 1 - Collect news
        run: python scripts/01_collect_news.py

      - name: Step 2 - Extract keywords & select topic
        run: python scripts/02_select_topic.py
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      - name: Step 3 - Generate blog post
        run: python scripts/03_generate_post.py
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          UNSPLASH_ACCESS_KEY: ${{ secrets.UNSPLASH_ACCESS_KEY }}
          
      - name: Step 4 - Post to Blogger
        run: python scripts/04_post_blogger.py
        env:
          BLOGGER_BLOG_ID: ${{ secrets.BLOGGER_BLOG_ID }}
          BLOGGER_CLIENT_ID: ${{ secrets.BLOGGER_CLIENT_ID }}
          BLOGGER_CLIENT_SECRET: ${{ secrets.BLOGGER_CLIENT_SECRET }}
          BLOGGER_REFRESH_TOKEN_2: ${{ secrets.BLOGGER_REFRESH_TOKEN_2 }} 

      - name: Step 5 - Add exam history section
        run: python scripts/06_exam_history.py
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}

      - name: Step 6 - Update topic history
        run: python scripts/05_update_history.py

      - name: Upload generated post as artifact
        uses: actions/upload-artifact@v4
        with:
          name: blog-post-${{ github.run_number }}
          path: |
            data/blog_post.html
            data/blog_post.json
            data/blog_post.md
          retention-days: 7

      - name: Commit updated history & post files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/topic_history.json data/blog_post.html data/blog_post.json data/blog_post.md
          git diff --staged --quiet || git commit -m "chore: auto-publish $(date +'%Y-%m-%d') [skip ci]"
          git push
