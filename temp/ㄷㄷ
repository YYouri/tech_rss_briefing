					<sshexec host="@{server.ip}"
							username="@{user}"
							password="@{passwd}"
							trust="true"
							outputproperty="result.@{server.ip}"
							command="BACKUP_DIR=${backup_root}/${task_id}; DRYRUN=${dryrun}; echo '${pathlist}' | while read -r p || [ -n "$p" ]; do [ -z "$p" ] && continue; echo "----- $p -----"; ls -l "$p" 2>&1; if [ -f "$p" ]; then if [ "$DRYRUN" = "true" ]; then echo "RESULT: WOULD_MOVE"; else mkdir -p "$BACKUP_DIR$(dirname "$p")"; mv "$p" "$BACKUP_DIR$p"; ls "$p" 2>&1; echo "RESULT: MOVED"; fi; else echo "RESULT: NOTFOUND"; fi; done"/>

또 깨질까봐 무서워